server:
  port: 34212
spring:
  application:
    name: consumer
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8976   # 上报端口
        dashboard: localhost:8080   # dashboard 端口


management:
  endpoints:
    web:
      exposure:
        include: "*"